## 术语定义

1. **子工程**: git submodule，有独立仓库，在本仓库以 submodule 形式引用
2. **子模块 / App**: Django app（backend/ 内的功能模块，如 users、projects）
3. **管理端 / 前端**: frontend/ 目录，面向由子 agent 驱动的 PC 管理界面
4. **H5 / 手机端**: h5/ 目录，由子 agent 驱动的移动用户侧
5. **后端**: backend/ 目录，由子 agent 驱动的 Django 后端